# Remove non-free repositories
d-i apt-setup/multiverse boolean false
d-i apt-setup/restricted boolean false

# Run post install script and remove it
ubiquity ubiquity/success_command string in-target --pass-stdout /root/postinst
